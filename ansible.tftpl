servers:
  hosts:
%{ for index,ip in ansible_host ~}
    server${index}:
      ansible_connection: "${ansible_connection}"
      ansible_host: "${ip}"
      ansible_port: "${ansible_port}"
      ansible_user: "${ansible_user}"
      ansible_ssh_private_key_file: "${ansible_ssh_private_key_file}"
%{ endfor ~}
